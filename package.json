{
  "name": "cra-tudisco",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "all": "npm run all --workspace=apps/web",
    "all:mobile": "npm run start --workspace=apps/mobile",
    "build": "npm run build --workspace=apps/web"
  }
}
